	private void format() {
		if (commit == null) {
			setDocument(new Document("")); 			return;
		}
		if (formatJob != null && formatJob.getState() != Job.NONE)
			formatJob.cancel();
		scheduleFormatJob();
	}

