	@Override
	protected void okPressed() {
		getButton(OK).setFocus(); 		helper.setRefFilters(filters);
		if (defaultsPerformed) {
			helper.resetLastSelectionStateToDefault();
		}
		super.okPressed();
	}

