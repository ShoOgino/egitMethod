		private void beforeHide() {
			lastText = toolbar.getText();
			lastSearchContext = searchContext;
			showStatus(toolbar, ""); 			toolbar = null;
			openCloseToggle.setChecked(false);
			wasVisible = false;
		}

