	private void addPersonIdent(StringBuilder d, PersonIdent ident,
			String label) {
		if (ident != null) {
			d.append(label).append(": "); 			d.append(ident.getName().trim());
			d.append(" <").append(ident.getEmailAddress().trim()).append("> "); 			d.append(dateFormatter.formatDate(ident));
			d.append(LF);
		}
	}

