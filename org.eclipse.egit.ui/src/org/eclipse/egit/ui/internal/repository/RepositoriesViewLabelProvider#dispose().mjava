	@Override
	public void dispose() {
		for (Image image : decoratedImages.values()) {
			image.dispose();
		}
		decoratedImages.clear();
		tagImage.dispose();
		lightweightTagImage.dispose();
		super.dispose();
	}

