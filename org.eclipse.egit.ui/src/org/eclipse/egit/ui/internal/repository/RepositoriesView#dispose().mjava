	@Override
	public void dispose() {
		if (this.scheduledJob != null) {
			this.scheduledJob.cancel();
			this.scheduledJob = null;
		}
		unregisterRepositoryListener();
		repositories.clear();
		super.dispose();
	}

