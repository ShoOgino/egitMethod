	private SyncRefEntity createSyncRepoEntity(String repoName, String ref) {
		String name = PATTERN.matcher(ref).replaceFirst(""); 
		if (name.startsWith(repoName + "/")) 			name = name.substring(repoName.length() + 1);

		return new SyncRefEntity(name, ref);
	}

