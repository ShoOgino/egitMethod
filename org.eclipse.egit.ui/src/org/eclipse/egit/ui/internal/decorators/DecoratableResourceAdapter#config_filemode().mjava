	private boolean config_filemode() {
		StoredConfig config = repository.getConfig();
		Boolean filemode = Boolean.valueOf(config.getBoolean("core", null, 				"filemode", true)); 		return filemode.booleanValue();
	}

