	private Set<String> migrateAbolutePaths() {
		String dirString;
		Set<String> dirs;
		dirString = prefs.get(PREFS_DIRECTORIES, ""); 		dirs = toDirSet(dirString);
		saveDirs(dirs);
		return dirs;
	}

