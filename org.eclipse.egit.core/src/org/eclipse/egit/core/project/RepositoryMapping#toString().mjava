	public String toString() {
		IPath absolutePath = getGitDirAbsolutePath();
		return "RepositoryMapping[" 				+ format(containerPathString)
				+ " -> '" 				+ format(gitDirPathString)
				+ "', absolute path: '"  				+ format(absolutePath) + "' ]"; 	}

